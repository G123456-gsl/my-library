<resources>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowBackground">@drawable/start_baground</item>
    </style>


    <style name="FullScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 隐藏状态栏 -->
<!--        <item name="android:windowFullscreen">true</item>-->
        <!-- 设置状态栏和导航栏颜色为透明 -->
<!--        <item name="android:statusBarColor">@android:color/transparent</item>-->
<!--        <item name="android:navigationBarColor">@android:color/transparent</item>-->
        <!-- 可选：让内容延伸到状态栏和导航栏区域 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="CustomDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

    <!-- 圆角ImageView -->
    <declare-styleable name="RoundCornerImageView">
        <attr name="corner_size" format="integer" />
        <attr name="top_left_size" format="integer" />
        <attr name="top_right_size" format="integer" />
        <attr name="bottom_left_size" format="integer" />
        <attr name="bottom_right_size" format="integer" />
    </declare-styleable>

</resources>
